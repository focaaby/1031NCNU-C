#include <stdio.h>
int main() {
    int i = 1;
    int n;
    scanf("%d", &n)
    while (i <=n) {
        printf("%d\n");
        i = i + 2;
    }
    for (i = 1; i<=j; i = i +1) {
        printf("%d", i);
    }
    return 0;
}
