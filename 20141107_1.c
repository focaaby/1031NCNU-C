#include <stdio.h>
//pointer is a data type like int, float, char etc.
//operators for pointer are &, *, +, -

int main() {
    int x = 0;
    printf("%d\n", x);
    scanf("%d", &x);
    printf("%d\n", x);
    printf("%p\n", &x)
    return 0;
}
